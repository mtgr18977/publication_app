{
  "name": "publication-app",
  "version": "1.0.0",
  "description": "A technical documentation platform made in node.",
  "main": "cria_menu.js",
  "scripts": {
    "start": "node cria_menu.js && npx serve .",
    "build-structure": "node cria_menu.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm run build-structure && npx serve -s"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "marked": "^4.0.0"
  }
}